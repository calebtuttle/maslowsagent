from setuptools import setup, find_packages

setup(
    name='maslowsagent',
    version='0.0.1',
    packages=find_packages(
        where='maslowsagent',
    ),
    install_requires=[
        '',
    ],
)